[toolchain]
# Rust >= 1.87 is required: some of Slint's transitive dependencies need edition2024 (1.85+)
# and the core uses `usize::is_multiple_of` (1.87). Tracking stable always satisfies that.
channel = "stable"
