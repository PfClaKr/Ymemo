<?xml version="1.0" encoding="utf-8"?>
<!-- The quick-write bar. This one takes its colors from the theme rather than a filter:
     nothing about it varies per memo. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/widget_pill" />
    <stroke android:width="1dp" android:color="@color/widget_divider" />
    <corners android:radius="26dp" />
</shape>
